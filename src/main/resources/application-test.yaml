spring:
  datasource:
    url: jdbc:h2:mem:hotel_test_db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
app:
  jwt-secret: dev-local-demo-jwt-secret-change-me-not-for-production-2026
  jwt-access-expiration-ms: 900000
  refresh-token-expiration-ms: 604800000
